# Release Notes

Planned release includes:

- Dashboard redesign (Phase F)
- WebSocket reliability and SSE fallback (Phase H)
- Accessibility checks (Phase K) via axe-core/jsdom and Playwright e2e scaffolding
- PWA baseline (Phase G), documentation and smoke tests
- Competitive analysis roadmap scaffolds for head-to-head, race strategy, mini-sectors, team radio, tyre degradation, overtake probability, results archive, track DNA, and consistency.
- Backend route coverage for the new comparison/archive endpoints, plus a running backend launch path that matches the repo layout.
